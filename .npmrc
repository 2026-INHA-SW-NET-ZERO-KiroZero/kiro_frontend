# Expo SDK 56 의존성 트리에 react-dom@19.2.7(react@19.2.4+ 요구)이 transitive로 끌려오는데
# 이 프로젝트는 react@19.2.3(RN 0.85 핀)에 고정되어 strict peer 검사에서 ERESOLVE가 난다.
# react-dom은 native 빌드에 불필요(web 전용)하므로 legacy peer 해석으로 lock 기준 설치를 허용한다.
# EAS Build의 npm ci도 이 설정을 따른다.
legacy-peer-deps=true
