# docs/references/

> 이 폴더의 역할: 외부 라이브러리 레퍼런스 보관소.
> 파일명 컨벤션: `{라이브러리명}-llms.txt` (예: `expo-router-llms.txt`, `zustand-llms.txt`).

에이전트가 라이브러리 사용법이 필요할 때, 매번 웹을 뒤지는 대신 여기에 모아둔 레퍼런스를 참조한다.
Expo는 버전마다 API가 바뀌므로(AGENTS.md), 반드시 **v56 기준** 문서를 받아 둔다.
