# docs/generated/

> 이 폴더의 파일은 **자동 생성**된다(`/gc`, `/health`, kiro-build). **직접 수정 금지.**
> 수정이 필요하면 소스 코드를 고친 뒤 재생성한다.

- `component-inventory.md` — 현재 컴포넌트 목록(자동 스캔).
- `api-schema.md` — API 명세(코드의 호출에서 추출. 현재는 더미라 빈 템플릿).
