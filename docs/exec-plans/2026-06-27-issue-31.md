# Issue #31 — API 연동: 조리 가이드 + 사용량 기록 + 정산 체크리스트

**이슈 URL**: https://github.com/2026-INHA-SW-NET-ZERO-KiroZero/kiro_frontend/issues/31  
**Labels**: feature  
**Milestone**: -

## 목표

`useSettlement()` 훅을 실제 API로 교체하고, 조리 가이드·사용량 기록·정산 체크리스트·세션 리포트 엔드포인트를 연동한다.

## 관련 파일

- `src/types/api/cookingGuide.ts` (신규)
- `src/lib/sessionApi.ts` (추가)
- `src/hooks/useCookingGuide.ts` (신규)
- `src/hooks/useSessionChecklist.ts` (신규)
- `src/hooks/useSessionResult.ts` (신규)
- `src/hooks/useSettlement.ts` (교체)
- `src/hooks/index.ts` (export 추가)
- `src/app/settlement.tsx` (체크리스트 UI로 재설계)
- `src/app/usage.tsx` (사진 업로드 + 소비율 제출 추가)
- `src/app/roomDetail.tsx` (settlement에 slotId 전달)
- `src/data/settlement.ts` (삭제)
- `src/data/index.ts` (export 제거)
- `src/types/index.ts` (새 타입 export)
- `src/types/settlement.ts` (삭제)

## 작업 분해

- [x] `CookingGuideResponse`, `SessionChecklistResponse` 타입 추가 (`src/types/api/cookingGuide.ts`)
- [x] `getCookingGuide`, `getSessionChecklist`, `getSessionResult` API 함수 (`src/lib/sessionApi.ts`)
- [x] `useCookingGuide(slotId)` 훅
- [x] `useSettlement(slotId)` → checklist API로 교체
- [x] `useSessionResult(slotId)` 훅
- [x] `settlement.tsx` 체크리스트 UI로 재설계
- [x] `usage.tsx` 사진 업로드 + 소비율 제출 추가
- [x] `roomDetail.tsx` settlement에 slotId 전달
- [x] `src/data/settlement.ts`, `src/types/settlement.ts` 삭제, data/index.ts 정리
- [x] harness 통과

## 완료 기준

- Swagger ↔ api-schema.md ↔ 훅 응답 타입 ↔ UI 4중 일치
- 업로드 응답 fileUrl → consumption-records의 cookedPhotoUrl/afterPhotoUrl 연결
- harness 통과
