# Issue #11 — 방 진행: AI 추천 + 사용량 기록 + 정산

**URL:** https://github.com/l2juhan/kiro_frontend/issues/11  
**Labels:** design  
**Milestone:** 2. 화면

## 목표

방 확정 후 진행 3화면(AI 추천 → 사용량 기록 → 정산)을 구현한다.

## 관련 파일

### 수정

- `src/app/recommend.tsx` — Placeholder → 풀 구현
- `src/app/usage.tsx` — Placeholder → 풀 구현
- `src/app/settlement.tsx` — Placeholder → 풀 구현

### 사용 훅

- `useMenuCandidates()` → `src/hooks/useMenus.ts`
- `useAggIngredients()` → `src/hooks/useAggIngredients.ts`
- `useSettlement(id)` → `src/hooks/useSettlement.ts`

## 작업 분해

### recommend (§3.5)

- [ ] 헤더 (뒤로 + "추천 메뉴")
- [ ] 크림 info 카드 (점수 = 소진율·추가구매 최소화·난이도)
- [ ] menuCandidates 3개 카드: 랭크칩(BEST 추천/후보 N) + 큰 점수+"점" + 이모지 타일 + 이름 + ⏱time·난이도
- [ ] 점수 바 (≥90 초록/≥85 골드/그 외 회색)
- [ ] 설명 + 필요 재료 칩 + 추가 구매 칩 (없으면 초록 "추가 구매 없이 가능해요")
- [ ] 선택 버튼 → roomDetail chosenMenu 설정

### usage (§3.6)

- [ ] 헤더 "사용량 기록" + 초록 info (실제 사용량→소진율·절감량 자동 계산)
- [ ] aggIngredients 리스트 + 편집 가능 수량 입력 (prefill)
- [ ] Sticky 초록 "조리 완료 처리하기" → heroStatus=COOKED, back

### settlement (§3.7)

- [ ] 빨강 그라데이션 hero (1인당 분담액·per·total·payer)
- [ ] 영수증 항목 + 합계
- [ ] 정산 현황 (payer "결제 완료" 칩 + debtors "N원 보냄")
- [ ] "균등분배 기준 자동 계산" 안내 문구
- [ ] 금액 toLocaleString('ko-KR')+'원' 포맷

## 완료 기준

- [ ] 완성 디자인과 픽셀 동일 (색·간격·라운딩·그림자·타이포·레이아웃)
- [ ] 한국어 카피는 PRD/프로토타입 문자열 그대로
- [ ] 라이트 모드 전용, 하드코딩 색 0건 (토큰만)
- [ ] SafeAreaView 사용, 비활성 상태는 회색으로 보이게
- [ ] 데이터는 커스텀 훅 경유 (src/data/ 직접 import 금지)
- [ ] ESLint + TypeScript 통과, kiro-qa 검증 통과
