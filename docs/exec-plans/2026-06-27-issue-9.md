# issue #9 — 홈 + Location 시트 (§3.3, §3.16)

> 작업 중 산출물. PR 생성 시 `git rm`으로 삭제 커밋에 포함한다.

## 대상

- `src/app/(tabs)/home.tsx` (현재 Placeholder → 본 구현)
- Location 바텀시트 오버레이 (§3.16)

## 사전 확인 (조사 완료)

로직/데이터 레이어가 이미 준비됨 → **UI 조립 + 표시 데이터 가공** 중심.

- 데이터: `useRooms()` (방 12개 = 16:00×6 + 18:00×6), `useMe()`, `useNotifications()`
- lib: `roomDisplay()` (배지/좌석 분기), theme `statusChip` (배지 색·라벨)
- 공용 컴포넌트: `StatusBadge`, `Avatar/AvatarStack`, `Chip/ToggleChip`, `SheetBase`, `Icon`
- 모든 색이 `theme.ts` 토큰에 매핑됨 (하드코딩 불필요)

## 디자인 원본 기준값 (KiroZero.dc.html)

- 홈 섹션: line 143~236 (`isHome`), `roomDisplay()` 로직 line 1650~1668
- Location 모달: line 1351~1371 (`showLocationModal`), 목록 line 1888
- 배지 분기 (line 1655~1660): CONFIRMED=`정원 마감 · 통합 완료`, COOKED=`조리 완료`, left≤0=`마감`, left≤1=`마감임박`, else=`N자리 남음`
- 카드 아바타: `parts.slice(0,3)`, label=이름 첫 글자, bg=parts[].c
- countText: `count/capacity명`
- 홈은 전부 OPEN 상태(프로토타입 초기값) → baseCount 사용

## 결정 사항

- 카드 탭 라우팅: `/roomDetail?id={id}` 로 통일 (roomDetail은 useRoomDetail(id) 기반)
- Location 목록 8개: 내 지역/서울/경기/인천/강원/대전·세종/충남/충북, 기본 선택=내 지역, 선택 시 빨강 check

## 파이프라인

1. **kiro-logic** — 홈 카드 표시 데이터 가공 (useRooms→roomDisplay 매핑, 추천 3개 슬라이스). 필요 시 `useHomeRooms()` 훅.
2. **kiro-ui** — `home.tsx` 본문(탑바·날짜스트립·필터칩·추천카드·오늘열린방 리스트) + Location 바텀시트.
3. **kiro-qa** — 디자인 픽셀 대조, 토큰만 사용/하드코딩 0건, 카피 원문, 훅 경유 검증.

## 완료 기준

- 픽셀 동일, 한국어 카피 원문, 라이트 모드 전용, 하드코딩 색 0건
- SafeAreaView 사용, 비활성=회색, 데이터 훅 경유
- ESLint + tsc 통과, kiro-qa 통과
