# issue #2 — 네비게이션 골격: Expo Router (auth·tabs·stack)

- **이슈**: https://github.com/2026-INHA-SW-NET-ZERO-KiroZero/kiro_frontend/issues/2
- **Labels**: feature
- **Milestone**: 1. 기반
- **작성일**: 2026-06-27

## 목표

앱이 `login`으로 부팅되고, 인증 성공 시 `home`으로, 탭/스택 전환이 PRD §2.4 모델대로 동작하는 네비게이션 골격. 후속 화면 이슈 전체의 선행 작업.

## 범위 결정 (이슈 본문 ↔ PRD 불일치)

이슈 #2 본문은 구버전으로 `admin`·`adminCreate`·`fridge`를 포함하나, 현재 **PRD §2.1**(단일 출처)에서 _"no Fridge tab/screen and no Admin screens — those features were removed"_. docs/FRONTEND.md §2와 최근 커밋(`c8984f0` admin 토큰 삭제)도 제거 확정.
→ **PRD 기준으로 진행**(사용자 확인 완료). admin·adminCreate·fridge 제외.

## 관련 파일

- `package.json` — main 엔트리, 의존성
- `app.json` — plugins(expo-router), scheme, typedRoutes
- `index.ts` / `App.tsx` — 제거(expo-router/entry로 대체)
- `babel.config.js` — babel-preset-expo (필요 시 생성)
- `src/app/**` — 라우트 골격 (신규)
- `src/theme/theme.ts` — 탭바 색 토큰 출처 (color.brand / color.textFaint2)
- `docs/FRONTEND.md` §2 — 폴더 구조 준수

## 작업 분해

- [ ] `npx expo install expo-router react-native-safe-area-context react-native-screens expo-linking expo-constants` (+ expo-status-bar 보유)
- [ ] `package.json` main → `expo-router/entry`, 구 `index.ts`/`App.tsx` 정리
- [ ] `app.json`에 `plugins: ["expo-router"]`, `scheme: "kirozero"`, `experiments.typedRoutes: true`
- [ ] `src/app/_layout.tsx` — root Stack, SafeAreaProvider, auth↔tabs 전환
- [ ] `(auth)/_layout.tsx` + `login`, `signup`
- [ ] `(tabs)/_layout.tsx` — 4탭(홈/내 모임/리포트/MY), 활성 `color.brand`+filled / 비활성 `color.textFaint2`+outline
- [ ] `(tabs)/home|meetings|report|my`
- [ ] stack: `roomDetail`,`recommend`,`usage`,`settlement`,`myApplication`,`pastApplication`,`pastEval`,`editProfile`,`notifications`
- [ ] 각 라우트 placeholder 스크린(SafeAreaView + 화면명)
- [ ] 탭 전환 시 스택 리셋 동작 확인(§2.4) — 각 탭 독립 stack 또는 push가 탭 위 native-stack
- [ ] `npm run harness` 통과, docs/FRONTEND.md 반영

## 완료 기준

- [ ] 부팅 → login, 로그인 → home, 탭 4개 전환, 스택 push/back 동작
- [ ] ESLint + TypeScript 에러 없음 (저장 시 Hook 통과)
- [ ] 탭바 활성/비활성 색이 theme 토큰 사용
