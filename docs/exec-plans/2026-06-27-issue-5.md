# issue #5 — (기반) 파생 계산 유틸 + 단위 테스트 (PRD §4.3)

- **이슈**: https://github.com/l2juhan/kiro_frontend/issues/5
- **Labels**: feature
- **Milestone**: 1. 기반

## 범위 조정 (2026-06-27, 사용자 확정)

- ❌ `derive.ts` **생성 안 함** — 환급 점수·환급 나뭇잎·리포트 co2는 **백엔드 계산값**을 받아옴(프론트 계산 아님).
- ❌ `emoji.ts` **제거** — 재료 마스터 100종에 아이콘 미사용(개별 생성 비현실적).
- ✅ `validators.ts` · `format.ts` · `roomDisplay.ts` 유지.
- ✅ **재료 마스터 100종** 추가 — 상위 폴더 `001_ingredient_master_seed.sql` → `src/data` 더미데이터 + `useIngredients()` 훅(API 교체 대비).
- ✅ 남은 함수 단위 테스트(경계값).

## 목표

폼 검증/표기/방 상태 분기를 `src/lib/`에 순수 함수로 모으고, 재료 마스터를 훅 레이어로 노출하며, 경계값 단위 테스트로 회귀를 잡는다.

## 작업 분해

- [ ] `src/lib/validators.ts` — 인하 이메일 도메인 `/@(inha\.ac\.kr|inha\.edu)$/i`, 폼 검증 유틸
- [ ] `src/lib/format.ts` — 돈 표기 `toLocaleString('ko-KR') + '원'`
- [ ] `src/lib/roomDisplay.ts` — 방 상태 → 배지/좌석/CTA 분기(§3.5)
- [ ] `src/lib/index.ts` — barrel
- [ ] `src/types`에 `Ingredient`(마스터) 타입 + 알레르겐 태그 타입
- [ ] `src/data/ingredients.ts` — 재료 마스터 100종 시드(SQL 출처 주석)
- [ ] `src/hooks/useIngredients.ts` — 마스터 목록 훅(기존 `useAggIngredients`와 분리)
- [ ] 단위 테스트: validators / format / roomDisplay 경계값(0/100/정원 마감/seatsLeft≤0)

## 완료 기준

- [ ] `npm test` 통과, 표기/분기 결과가 PRD 카피와 일치
- [ ] ESLint + TypeScript 통과(`any` 없음)
- [ ] UI는 `src/data` 직접 import 금지 — `useIngredients()` 경유
