# issue #13 — 지난 모임 + 모임 평가 (§3.10–3.11)

대상 화면: `pastApplication`, `pastEval`. 디자인 출처: `FRONTEND_PRD.md` §3.10·§3.11·§4.3, `KiroZero.dc.html`.

## 산출물

1. `assets/cook-hero.png` — 디자인 핸드오프에서 복사(인증 사진/완성 메뉴 프록시).
2. `src/theme/theme.ts` — pastApplication/pastEval 고유 색 토큰 추가(red CTA·완료 배너·탄소 슬랩 링·바 트랙·환급 점 등).
3. `src/lib/derive.ts` — 환급 점수/나뭇잎 계산(PRD §4.3). 순수 함수.
4. `src/data/pastMeetings.ts` — `pastEvaluatedSeed`(past2 사전 평가) export 추가.
5. `src/hooks/usePastEval.tsx` — `pastEvaluated` 전역 상태 Context(useProfile 동일 패턴). `_layout.tsx`에 Provider 연결, `hooks/index.ts` barrel.
6. `src/app/pastApplication.tsx` — §3.10.
7. `src/app/pastEval.tsx` — §3.11.

## 상태/파생 (PRD §4.3, dc.html computed 1854–1885)

- `myBrought = parts.find(isMe).brought.split(' · ')`
- `useScore = round(30 * useAvg/100)`, `eatScore = round(30 * food/100)`
- `refundScore = 40 + useScore + eatScore`
- `refundCredit = round(2000 * 0.5 * refundScore/100)`, reserve = 2000
- 평가 완료 표시값은 `seedEval`(food, use[name]) 사용, 없으면 `past.rate` fallback.
- 평가 입력(사진2·음식%·재료별%)은 화면-로컬 useState. 제출 시 `markEvaluated(id)` + back.

## 결정

- 참여자 skill 칩은 전역 `SkillChip` 컴포넌트(토큰 단일 출처) 재사용 — dc.html 과거화면이 중급을 갈색으로 하드코딩했으나, 앱 일관성 위해 `skillChip` 토큰을 따른다.
- 링은 `ProgressRing`(SVG)으로 conic-gradient 대체.
- 라우트 param `id`로 지난 모임 선택, 없으면 첫 항목.
