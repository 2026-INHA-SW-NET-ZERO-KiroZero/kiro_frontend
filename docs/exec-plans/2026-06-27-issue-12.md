# issue #12 — 내 모임: 캘린더 + 신청 내역 4단계 (§3.8–3.9)

대상 화면: `(tabs)/meetings`(캘린더), `myApplication`(4단계)

## 데이터/훅 (모두 기존 재사용)

- `useMyApplications()` — 다가오는 모임 카드, 신청 단건은 `useMyApplication(id)`.
- `usePastMeetings()` — 지난 모임 카드.
- `usePartyPool()` — recruiting 단계 참여자 카드.
- `useVoteMenus()` / `useDecidedMenu()` — voting / result 단계.

신규 더미데이터·훅 없음. 화면 파생(stage/뱃지/캘린더 그리드)은 feature 내부 로컬 계산.

## theme.ts 확장

dc.html `myApplication`에 토큰화되지 않은 색 7종 존재 → 하드코딩 금지 준수를 위해
`applicationStage` 토큰 그룹 신설(임의값 아닌 핸드오프 추출값).

## 작업 순서

1. `docs/exec-plans/2026-06-27-issue-12.md` (본 파일)
2. `src/theme/theme.ts` — `applicationStage` 토큰 추가
3. `src/features/meetings/calendar.ts` — 2026-06 월 그리드 빌더(pure)
4. `src/features/meetings/MeetingsScreen.tsx` — 캘린더 + 다가오는/지난 모임
5. `src/app/(tabs)/meetings.tsx` — 라우트 연결
6. `src/features/meetings/MyApplicationScreen.tsx` — 4단계 조건부 렌더
7. `src/app/myApplication.tsx` — 라우트 연결
8. ESLint + tsc 통과 → 커밋

## stage 파생 규칙 (PRD §3.9)

`canceled` → `recruiting`(미달) → `voting`(마감 & !votingDone) → `result`(마감 & votingDone)

- voting: myVote(0..3 일반, 4=E), E 선택 시 재추천 사유 textarea 필수, CTA는 선택 전 비활성.
- 일반 투표 제출 → votingDone=true(→result), E 제출 → 투표 리셋(재추천).
